#include "main.h"
/**
 * _isdigit - evaluate if is a digit
 * @c: print int
 * Return: ALways 0 (success/correct)
 */

int _isdigit(int c)
{
if (c >= 48 && c <=57)
{
return (1);
}


else
{
return (0);
}
}
