#include "main.h"
/**
 * _isdigit - evaluate if is a digit
 * @c: print int
 * Return: 1 if the number is a digit, else 0.
 */

int _isdigit(int c)
{
if (c >=  '48' && c <= '57')
{
return (1);
}

else
{
return (0);
}
}
